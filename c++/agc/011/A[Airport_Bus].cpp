#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
  ll N, C, K;
  cin >> N >> C >> K;

  vector<ll> T(N);
  for (ll i = 0; i < N; i++) {
    cin >> T[i];
  }

  sort(T.begin(), T.end());

  int res = 0, i = 0;

  while (i < N) {
    res++;
  }
}
