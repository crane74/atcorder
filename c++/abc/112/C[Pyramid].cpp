#include <bits/stdc++.h>
using namespace std;
using ll = long long;

//よくわからない

int main() {
  int N;
  cin >> N;

  vector<int> x(N, 0), y(N, 0), h(N, 0);
  for (int i = 0; i < N; i++) {
    cin >> x[i] >> y[i] >> h[i];
  }

  ll X, Y, H;
}