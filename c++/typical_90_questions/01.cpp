#include <bits/stdc++.h>
using namespace std;

//各値の宣言
int N;          //全体の切れ目の数
int L;          //長さ
int K;          //選ぶ切れ目の数
vector<int> A;  //入力する長さ

int main() {
  // Step1 入力
  cin >> N >> L >> K;

  for (int i = 0; i < N; i++) {
    cin >> A.at(i);
  }

  // Step2 出力
}